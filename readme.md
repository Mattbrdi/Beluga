## How to install the pipeline

1. Create conda environnement `conda create -n nenv python=3.10 pip` Version is really important for compatibility between different modules

2. Activate the virtual environement with `conda activate nenv`

3. python -m pip install --upgrade pip

4. python -m pip install -r pipeline\requirements.txt



